<Window x:Class="MiniKartoteka.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://www.codeplex.com/prism"
        xmlns:inf="clr-namespace:MiniKartoteka.Infrastructure;assembly=MiniKartoteka.Infrastructure"
        xmlns:local="clr-namespace:MiniKartoteka" 
        Width="400" Height="400" mc:Ignorable="d" Title="Shell">
    <Window.Resources>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Header" Value="{Binding DataContext.Title}"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <TabControl prism:RegionManager.RegionName="{x:Static inf:RegionNames.TABCONTROL_REGION}"/>
        <ContentControl prism:RegionManager.RegionName="{x:Static inf:RegionNames.STATUSBAR_REGION}" Grid.Row="1"/>

        <Button Grid.Row="1" HorizontalAlignment="Right" Command="{Binding Navigate}">NAVIGUJ</Button>
    </Grid>
</Window>
